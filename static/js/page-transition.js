(function () { const root = document.documentElement; const transitionKey = "nuaa-page-transition"; const exitDelay = 140; const reduceMotion = window.matchMedia("(prefers-reduced-motion: reduce)"); const finishEnter = () => { if (!root.classList.contains("is-page-entering")) { return; } requestAnimationFrame(() => { requestAnimationFrame(() => { root.classList.remove("is-page-entering"); }); }); }; const isPlainLeftClick = (event) => event.button === 0 && !event.defaultPrevented && !event.metaKey && !event.ctrlKey && !event.shiftKey && !event.altKey && event.detail !== 0; const shouldAnimateLink = (anchor) => { if (!anchor || reduceMotion.matches) { return false; } if (anchor.dataset.noTransition === "true") { return false; } if (anchor.target && anchor.target.toLowerCase() !== "_self") { return false; } if (anchor.hasAttribute("download")) { return false; } const rawHref = anchor.getAttribute("href") || ""; if (!rawHref || /^(mailto|tel|sms|javascript):/i.test(rawHref)) { return false; } const nextUrl = new URL(anchor.href, window.location.href); const currentUrl = new URL(window.location.href); if (nextUrl.origin !== currentUrl.origin) { return false; } if ( nextUrl.pathname === currentUrl.pathname && nextUrl.search === currentUrl.search && nextUrl.hash ) { return false; } return true; }; const startExit = (href) => { try { sessionStorage.setItem(transitionKey, "1"); } catch {} root.classList.add("is-changing"); window.setTimeout(() => { window.location.href = href; }, exitDelay); }; if (document.readyState === "loading") { document.addEventListener("DOMContentLoaded", finishEnter, { once: true }); } else { finishEnter(); } window.addEventListener("pageshow", () => { root.classList.remove("is-changing"); finishEnter(); }); document.addEventListener("click", (event) => { if (!isPlainLeftClick(event)) { return; } const anchor = event.target.closest("a[href]"); if (!shouldAnimateLink(anchor)) { return; } event.preventDefault(); startExit(anchor.href); }); })();
